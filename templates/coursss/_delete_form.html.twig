<form method="post" action="{{ path('coursss_delete', {'id': coursss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coursss.id) }}">
    <button class="btn">Delete</button>
</form>
