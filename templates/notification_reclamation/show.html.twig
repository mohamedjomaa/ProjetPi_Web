


    <h1>NotificationReclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notification_reclamation.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ notification_reclamation.etat }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ notification_reclamation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('notification_reclamation_index') }}">back to list</a>

    <a href="{{ path('notification_reclamation_edit', {'id': notification_reclamation.id}) }}">edit</a>

    {{ include('notification_reclamation/_delete_form.html.twig') }}

